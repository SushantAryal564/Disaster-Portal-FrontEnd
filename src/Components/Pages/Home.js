import React from "react";
import Layout from "../Layout/Layout";
import NavigationBar from "../Layout/NavBar";
const Home = (props) => {
  return (
    <Layout>
      <div>Home Page</div>
    </Layout>
  );
};
export default Home;
